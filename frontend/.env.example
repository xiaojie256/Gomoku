# Backend API URL for Express.js server
# This ensures frontend requests hit the Express backend instead of Next.js Route Handlers
# If not set, Next.js rewrites in next.config.js will proxy /api/* requests to the backend
NEXT_PUBLIC_API_URL=http://localhost:4125
